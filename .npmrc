# Publish scoped packages as public by default
access=public

# Uncomment and set these to publish to a private registry:
# @identity-verification:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${NPM_TOKEN}
